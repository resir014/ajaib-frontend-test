import * as React from 'react';
import type { NextPage } from 'next';
import { LayoutRoot } from '~/components/layout/layout-root';

const IndexPage: NextPage = () => {
  return <LayoutRoot>layout</LayoutRoot>;
};

export default IndexPage;
